name: Keep Supabase project awake

on:
  schedule:
    - cron: "0 15 * * 1,4"   # every Monday and Thursday at 15:00 UTC
  workflow_dispatch:          # lets you run it manually from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the database
        run: |
          curl -s -o /dev/null -w "HTTP %{http_code}\n" \
            "https://qstztxydqhuahgivpztx.supabase.co/rest/v1/places?select=id&limit=1" \
            -H "apikey: sb_publishable_5MfonGtWBM7R3rgYtEDdkg_TnUOeWJx" \
            -H "Authorization: Bearer sb_publishable_5MfonGtWBM7R3rgYtEDdkg_TnUOeWJx"
